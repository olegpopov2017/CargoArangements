import {scene,helper,} from './main';

export class Box
{
  // length = 1;
  // width = 1;
  // height = 1;
  // x = 1;
  // y = 1;
  // z = 1;
  name = 'box_json';
  constructor(length,width,height,x,y,z) 
  {
    this.length = length;
    this.width = width;
    this.height = height;    
    this.x = x;
    this.y = y;
    this.z = z;
  }
};



